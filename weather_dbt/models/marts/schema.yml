version: 2

models:
  - name: dim_weather_stations
    columns:
      - name: station_id
        tests:
          - unique
          - not_null

  - name: fct_weather_observations
    columns:
      - name: station_id
        tests:
          - not_null

      - name: observation_time
        tests:
          - not_null